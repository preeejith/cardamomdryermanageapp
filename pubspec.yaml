name: cardamom_dryer_app
description: Cardamom Dryer Management Application
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase - UPDATED TO LATEST VERSIONS
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  firebase_storage: ^12.3.4
  firebase_messaging: ^15.1.3
  cloud_functions: ^5.1.3
  
  # State Management
  provider: ^6.1.2
  
  # UI & Navigation
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  intl: ^0.19.0
  
  # PDF Generation
  pdf: ^3.11.1
  printing: ^5.13.3
  path_provider: ^2.1.4
  
  # URL Launcher (WhatsApp)
  url_launcher: ^6.3.1
  
  # Phone Number Input
  intl_phone_field: ^3.2.0
  
  # Loading & Dialog
  flutter_spinkit: ^5.2.1
  
  # Image Picker
  image_picker: ^1.1.2
  
  # Shared Preferences
  shared_preferences: ^2.3.2
  
  # UUID
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  
  # Assets
  # assets:
  #   - images/
  #   - assets/